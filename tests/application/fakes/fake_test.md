# usecase_testを定義

## fakeを定義する意味
実際にそのメソッドを呼び出すのではなく、呼び出されたかどうかを確認するためのfakeを用意する。

## fakeを利用をするケース
- usecaseのテストで、依存しているportのメソッドが呼び出されたかどうかを確認したい場合


## fakeのファイルフォーマット

```python

# coding: utf-8
# $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
# 仕様：
# 値をダミーで渡して流れを確認するためにFakeを定義
#* 値に関しては制約を満たすように最低限の実装のみ
# $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
# import


# ----------------------------------------------------------------------------------
# **********************************************************************************

class XXX:
    def __init__(self):
        self.called = False  # 処理が呼ばれたか確認
        self.received = None

    def execute(self, req):
        pass
    
# **********************************************************************************

```

